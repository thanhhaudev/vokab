/// vokab core logic package.
///
/// Holds everything that is independent of AppKit/SwiftUI: the agy CLI wrapper,
/// card models, GRDB storage, SM-2 spaced repetition, and capture orchestration.
/// This package is unit-testable offline (no agy, no network).
public enum VokabKit {
    public static let version = "0.0.1"
}
